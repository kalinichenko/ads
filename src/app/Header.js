import React from 'react';
import styles from './Header.css';

export default () => (
  <header className={styles.header}>
    <a href="https://www.olx.com" className={styles.logo}></a>
  </header>
);
